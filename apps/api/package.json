{
  "name": "@shrapp/api",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "generate": "drizzle-kit generate",
    "migrate:local": "wrangler d1 migrations apply shrapp-db --local",
    "migrate:remote": "wrangler d1 migrations apply shrapp-db --remote",
    "lint": "echo 'no linter configured'"
  },
  "dependencies": {
    "drizzle-orm": "^0.44.0",
    "hono": "^4.7.0",
    "zod": "^3.25.0",
    "@shrapp/shared": "workspace:*"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250410.0",
    "drizzle-kit": "^0.31.0",
    "typescript": "~5.8.3",
    "wrangler": "^4.14.0"
  }
}
